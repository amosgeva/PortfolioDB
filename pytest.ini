[pytest]
# Registered so `-m slow` does not warn about an unknown marker.
markers =
    slow: needs a live Postgres and takes tens of seconds — excluded from the
        pre-commit hook, run deliberately with `-m slow`.
